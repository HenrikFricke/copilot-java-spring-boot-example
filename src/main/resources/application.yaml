spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
